import styled from 'styled-components'

export const IconContainer = styled.button`
  border: none;
  background: none;
  color: inherit;
  font-size: 1.5rem;

  svg {
    display: inherit;
    cursor: pointer;
  }
`
